services:
  - type: web
    name: urna_flet
    runtime: python
    buildCommand: ""
    startCommand: flet run src/main.py --port $PORT --web
    envVars:
      - key: FLET_WEB_AUTO_RELOAD
        value: "false"
